// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.32.0
// source: build/proto/build.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../google/protobuf/any";
import { FileDescriptorProto } from "../../google/protobuf/descriptor";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "proto";

export enum CurrentBuildStatus {
  BUILD_STATE_UNSPECIFIED = 0,
  BUILD_STATE_NOT_FOUND = 1,
  BUILD_STATE_QUEUED = 2,
  BUILD_STATE_RUNNING = 3,
  BUILD_STATE_SUCCESS = 4,
  BUILD_STATE_FAILURE = 5,
  UNRECOGNIZED = -1,
}

export function currentBuildStatusFromJSON(object: any): CurrentBuildStatus {
  switch (object) {
    case 0:
    case "BUILD_STATE_UNSPECIFIED":
      return CurrentBuildStatus.BUILD_STATE_UNSPECIFIED;
    case 1:
    case "BUILD_STATE_NOT_FOUND":
      return CurrentBuildStatus.BUILD_STATE_NOT_FOUND;
    case 2:
    case "BUILD_STATE_QUEUED":
      return CurrentBuildStatus.BUILD_STATE_QUEUED;
    case 3:
    case "BUILD_STATE_RUNNING":
      return CurrentBuildStatus.BUILD_STATE_RUNNING;
    case 4:
    case "BUILD_STATE_SUCCESS":
      return CurrentBuildStatus.BUILD_STATE_SUCCESS;
    case 5:
    case "BUILD_STATE_FAILURE":
      return CurrentBuildStatus.BUILD_STATE_FAILURE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CurrentBuildStatus.UNRECOGNIZED;
  }
}

export function currentBuildStatusToJSON(object: CurrentBuildStatus): string {
  switch (object) {
    case CurrentBuildStatus.BUILD_STATE_UNSPECIFIED:
      return "BUILD_STATE_UNSPECIFIED";
    case CurrentBuildStatus.BUILD_STATE_NOT_FOUND:
      return "BUILD_STATE_NOT_FOUND";
    case CurrentBuildStatus.BUILD_STATE_QUEUED:
      return "BUILD_STATE_QUEUED";
    case CurrentBuildStatus.BUILD_STATE_RUNNING:
      return "BUILD_STATE_RUNNING";
    case CurrentBuildStatus.BUILD_STATE_SUCCESS:
      return "BUILD_STATE_SUCCESS";
    case CurrentBuildStatus.BUILD_STATE_FAILURE:
      return "BUILD_STATE_FAILURE";
    case CurrentBuildStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Hash {
  value: string;
}

export interface Definition {
  /**
   * A canonical type name, e.g. "tinyrange/v1/fetch_http".
   * This is the dispatch key for the registry.
   */
  typeName: string;
  /**
   * The raw payload, encoded as an Any so it can carry arbitrary
   * protobuf messages defined by third parties.
   */
  payload: Any | undefined;
}

export interface DefinitionReference {
  /** The hash of the referenced definition. */
  hash: string;
}

export interface BuildClosure {
  /** The root definition of the build. */
  root:
    | Definition
    | undefined;
  /** All direct dependencies of the root definition. */
  dependencies: Definition[];
}

export interface BuildReceipt {
  /** The hash of the build itself. */
  hash:
    | Hash
    | undefined;
  /** Direct dependencies of the build, identified by their hashes. */
  dependencies: Hash[];
  /** Start time of the build. */
  startTime:
    | Date
    | undefined;
  /** End time of the build. */
  endTime:
    | Date
    | undefined;
  /** Files produced by the build, secured with their hashes. */
  outputs: { [key: string]: Hash };
}

export interface BuildReceipt_OutputsEntry {
  key: string;
  value: Hash | undefined;
}

export interface BuildRequest {
  /** The build closure to build. */
  closure: BuildClosure | undefined;
}

export interface BuildStatus {
  /** The build has completed successfully. */
  success?: BuildReceipt | undefined;
}

export interface BuilderMetadata {
  /** The canonical type name of the builder, e.g. "tinyrange/v1/fetch_http". */
  typeName: string;
  /** The top-level protobuf type that the builder accepts as its definition. */
  topLevelType: string;
  /** The protobuf type of the builder's definition message. */
  definition: FileDescriptorProto | undefined;
}

export interface BuilderList {
  builders: BuilderMetadata[];
}

export interface BuildStatusResponse {
  statuses: { [key: string]: CurrentBuildStatus };
}

export interface BuildStatusResponse_StatusesEntry {
  key: string;
  value: CurrentBuildStatus;
}

export interface HashRequest {
  definitions: Definition[];
}

export interface HashResponse {
  hashes: Hash[];
  statuses: CurrentBuildStatus[];
}

function createBaseHash(): Hash {
  return { value: "" };
}

export const Hash: MessageFns<Hash> = {
  encode(message: Hash, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Hash {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHash();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Hash {
    return { value: isSet(object.value) ? globalThis.String(object.value) : "" };
  },

  toJSON(message: Hash): unknown {
    const obj: any = {};
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Hash>, I>>(base?: I): Hash {
    return Hash.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Hash>, I>>(object: I): Hash {
    const message = createBaseHash();
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseDefinition(): Definition {
  return { typeName: "", payload: undefined };
}

export const Definition: MessageFns<Definition> = {
  encode(message: Definition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.typeName !== "") {
      writer.uint32(10).string(message.typeName);
    }
    if (message.payload !== undefined) {
      Any.encode(message.payload, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Definition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.typeName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Definition {
    return {
      typeName: isSet(object.typeName) ? globalThis.String(object.typeName) : "",
      payload: isSet(object.payload) ? Any.fromJSON(object.payload) : undefined,
    };
  },

  toJSON(message: Definition): unknown {
    const obj: any = {};
    if (message.typeName !== "") {
      obj.typeName = message.typeName;
    }
    if (message.payload !== undefined) {
      obj.payload = Any.toJSON(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Definition>, I>>(base?: I): Definition {
    return Definition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Definition>, I>>(object: I): Definition {
    const message = createBaseDefinition();
    message.typeName = object.typeName ?? "";
    message.payload = (object.payload !== undefined && object.payload !== null)
      ? Any.fromPartial(object.payload)
      : undefined;
    return message;
  },
};

function createBaseDefinitionReference(): DefinitionReference {
  return { hash: "" };
}

export const DefinitionReference: MessageFns<DefinitionReference> = {
  encode(message: DefinitionReference, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hash !== "") {
      writer.uint32(10).string(message.hash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DefinitionReference {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDefinitionReference();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hash = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DefinitionReference {
    return { hash: isSet(object.hash) ? globalThis.String(object.hash) : "" };
  },

  toJSON(message: DefinitionReference): unknown {
    const obj: any = {};
    if (message.hash !== "") {
      obj.hash = message.hash;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DefinitionReference>, I>>(base?: I): DefinitionReference {
    return DefinitionReference.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DefinitionReference>, I>>(object: I): DefinitionReference {
    const message = createBaseDefinitionReference();
    message.hash = object.hash ?? "";
    return message;
  },
};

function createBaseBuildClosure(): BuildClosure {
  return { root: undefined, dependencies: [] };
}

export const BuildClosure: MessageFns<BuildClosure> = {
  encode(message: BuildClosure, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.root !== undefined) {
      Definition.encode(message.root, writer.uint32(10).fork()).join();
    }
    for (const v of message.dependencies) {
      Definition.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuildClosure {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuildClosure();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.root = Definition.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dependencies.push(Definition.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuildClosure {
    return {
      root: isSet(object.root) ? Definition.fromJSON(object.root) : undefined,
      dependencies: globalThis.Array.isArray(object?.dependencies)
        ? object.dependencies.map((e: any) => Definition.fromJSON(e))
        : [],
    };
  },

  toJSON(message: BuildClosure): unknown {
    const obj: any = {};
    if (message.root !== undefined) {
      obj.root = Definition.toJSON(message.root);
    }
    if (message.dependencies?.length) {
      obj.dependencies = message.dependencies.map((e) => Definition.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuildClosure>, I>>(base?: I): BuildClosure {
    return BuildClosure.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuildClosure>, I>>(object: I): BuildClosure {
    const message = createBaseBuildClosure();
    message.root = (object.root !== undefined && object.root !== null)
      ? Definition.fromPartial(object.root)
      : undefined;
    message.dependencies = object.dependencies?.map((e) => Definition.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBuildReceipt(): BuildReceipt {
  return { hash: undefined, dependencies: [], startTime: undefined, endTime: undefined, outputs: {} };
}

export const BuildReceipt: MessageFns<BuildReceipt> = {
  encode(message: BuildReceipt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hash !== undefined) {
      Hash.encode(message.hash, writer.uint32(10).fork()).join();
    }
    for (const v of message.dependencies) {
      Hash.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.startTime !== undefined) {
      Timestamp.encode(toTimestamp(message.startTime), writer.uint32(26).fork()).join();
    }
    if (message.endTime !== undefined) {
      Timestamp.encode(toTimestamp(message.endTime), writer.uint32(34).fork()).join();
    }
    Object.entries(message.outputs).forEach(([key, value]) => {
      BuildReceipt_OutputsEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuildReceipt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuildReceipt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hash = Hash.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dependencies.push(Hash.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.endTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = BuildReceipt_OutputsEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.outputs[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuildReceipt {
    return {
      hash: isSet(object.hash) ? Hash.fromJSON(object.hash) : undefined,
      dependencies: globalThis.Array.isArray(object?.dependencies)
        ? object.dependencies.map((e: any) => Hash.fromJSON(e))
        : [],
      startTime: isSet(object.startTime) ? fromJsonTimestamp(object.startTime) : undefined,
      endTime: isSet(object.endTime) ? fromJsonTimestamp(object.endTime) : undefined,
      outputs: isObject(object.outputs)
        ? Object.entries(object.outputs).reduce<{ [key: string]: Hash }>((acc, [key, value]) => {
          acc[key] = Hash.fromJSON(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: BuildReceipt): unknown {
    const obj: any = {};
    if (message.hash !== undefined) {
      obj.hash = Hash.toJSON(message.hash);
    }
    if (message.dependencies?.length) {
      obj.dependencies = message.dependencies.map((e) => Hash.toJSON(e));
    }
    if (message.startTime !== undefined) {
      obj.startTime = message.startTime.toISOString();
    }
    if (message.endTime !== undefined) {
      obj.endTime = message.endTime.toISOString();
    }
    if (message.outputs) {
      const entries = Object.entries(message.outputs);
      if (entries.length > 0) {
        obj.outputs = {};
        entries.forEach(([k, v]) => {
          obj.outputs[k] = Hash.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuildReceipt>, I>>(base?: I): BuildReceipt {
    return BuildReceipt.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuildReceipt>, I>>(object: I): BuildReceipt {
    const message = createBaseBuildReceipt();
    message.hash = (object.hash !== undefined && object.hash !== null) ? Hash.fromPartial(object.hash) : undefined;
    message.dependencies = object.dependencies?.map((e) => Hash.fromPartial(e)) || [];
    message.startTime = object.startTime ?? undefined;
    message.endTime = object.endTime ?? undefined;
    message.outputs = Object.entries(object.outputs ?? {}).reduce<{ [key: string]: Hash }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = Hash.fromPartial(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBaseBuildReceipt_OutputsEntry(): BuildReceipt_OutputsEntry {
  return { key: "", value: undefined };
}

export const BuildReceipt_OutputsEntry: MessageFns<BuildReceipt_OutputsEntry> = {
  encode(message: BuildReceipt_OutputsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      Hash.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuildReceipt_OutputsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuildReceipt_OutputsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Hash.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuildReceipt_OutputsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? Hash.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: BuildReceipt_OutputsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = Hash.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuildReceipt_OutputsEntry>, I>>(base?: I): BuildReceipt_OutputsEntry {
    return BuildReceipt_OutputsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuildReceipt_OutputsEntry>, I>>(object: I): BuildReceipt_OutputsEntry {
    const message = createBaseBuildReceipt_OutputsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null) ? Hash.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseBuildRequest(): BuildRequest {
  return { closure: undefined };
}

export const BuildRequest: MessageFns<BuildRequest> = {
  encode(message: BuildRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.closure !== undefined) {
      BuildClosure.encode(message.closure, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuildRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuildRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.closure = BuildClosure.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuildRequest {
    return { closure: isSet(object.closure) ? BuildClosure.fromJSON(object.closure) : undefined };
  },

  toJSON(message: BuildRequest): unknown {
    const obj: any = {};
    if (message.closure !== undefined) {
      obj.closure = BuildClosure.toJSON(message.closure);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuildRequest>, I>>(base?: I): BuildRequest {
    return BuildRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuildRequest>, I>>(object: I): BuildRequest {
    const message = createBaseBuildRequest();
    message.closure = (object.closure !== undefined && object.closure !== null)
      ? BuildClosure.fromPartial(object.closure)
      : undefined;
    return message;
  },
};

function createBaseBuildStatus(): BuildStatus {
  return { success: undefined };
}

export const BuildStatus: MessageFns<BuildStatus> = {
  encode(message: BuildStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== undefined) {
      BuildReceipt.encode(message.success, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuildStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuildStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.success = BuildReceipt.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuildStatus {
    return { success: isSet(object.success) ? BuildReceipt.fromJSON(object.success) : undefined };
  },

  toJSON(message: BuildStatus): unknown {
    const obj: any = {};
    if (message.success !== undefined) {
      obj.success = BuildReceipt.toJSON(message.success);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuildStatus>, I>>(base?: I): BuildStatus {
    return BuildStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuildStatus>, I>>(object: I): BuildStatus {
    const message = createBaseBuildStatus();
    message.success = (object.success !== undefined && object.success !== null)
      ? BuildReceipt.fromPartial(object.success)
      : undefined;
    return message;
  },
};

function createBaseBuilderMetadata(): BuilderMetadata {
  return { typeName: "", topLevelType: "", definition: undefined };
}

export const BuilderMetadata: MessageFns<BuilderMetadata> = {
  encode(message: BuilderMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.typeName !== "") {
      writer.uint32(10).string(message.typeName);
    }
    if (message.topLevelType !== "") {
      writer.uint32(18).string(message.topLevelType);
    }
    if (message.definition !== undefined) {
      FileDescriptorProto.encode(message.definition, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuilderMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuilderMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.typeName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.topLevelType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.definition = FileDescriptorProto.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuilderMetadata {
    return {
      typeName: isSet(object.typeName) ? globalThis.String(object.typeName) : "",
      topLevelType: isSet(object.topLevelType) ? globalThis.String(object.topLevelType) : "",
      definition: isSet(object.definition) ? FileDescriptorProto.fromJSON(object.definition) : undefined,
    };
  },

  toJSON(message: BuilderMetadata): unknown {
    const obj: any = {};
    if (message.typeName !== "") {
      obj.typeName = message.typeName;
    }
    if (message.topLevelType !== "") {
      obj.topLevelType = message.topLevelType;
    }
    if (message.definition !== undefined) {
      obj.definition = FileDescriptorProto.toJSON(message.definition);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuilderMetadata>, I>>(base?: I): BuilderMetadata {
    return BuilderMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuilderMetadata>, I>>(object: I): BuilderMetadata {
    const message = createBaseBuilderMetadata();
    message.typeName = object.typeName ?? "";
    message.topLevelType = object.topLevelType ?? "";
    message.definition = (object.definition !== undefined && object.definition !== null)
      ? FileDescriptorProto.fromPartial(object.definition)
      : undefined;
    return message;
  },
};

function createBaseBuilderList(): BuilderList {
  return { builders: [] };
}

export const BuilderList: MessageFns<BuilderList> = {
  encode(message: BuilderList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.builders) {
      BuilderMetadata.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuilderList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuilderList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.builders.push(BuilderMetadata.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuilderList {
    return {
      builders: globalThis.Array.isArray(object?.builders)
        ? object.builders.map((e: any) => BuilderMetadata.fromJSON(e))
        : [],
    };
  },

  toJSON(message: BuilderList): unknown {
    const obj: any = {};
    if (message.builders?.length) {
      obj.builders = message.builders.map((e) => BuilderMetadata.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuilderList>, I>>(base?: I): BuilderList {
    return BuilderList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuilderList>, I>>(object: I): BuilderList {
    const message = createBaseBuilderList();
    message.builders = object.builders?.map((e) => BuilderMetadata.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBuildStatusResponse(): BuildStatusResponse {
  return { statuses: {} };
}

export const BuildStatusResponse: MessageFns<BuildStatusResponse> = {
  encode(message: BuildStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    Object.entries(message.statuses).forEach(([key, value]) => {
      BuildStatusResponse_StatusesEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuildStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuildStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = BuildStatusResponse_StatusesEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.statuses[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuildStatusResponse {
    return {
      statuses: isObject(object.statuses)
        ? Object.entries(object.statuses).reduce<{ [key: string]: CurrentBuildStatus }>((acc, [key, value]) => {
          acc[key] = currentBuildStatusFromJSON(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: BuildStatusResponse): unknown {
    const obj: any = {};
    if (message.statuses) {
      const entries = Object.entries(message.statuses);
      if (entries.length > 0) {
        obj.statuses = {};
        entries.forEach(([k, v]) => {
          obj.statuses[k] = currentBuildStatusToJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuildStatusResponse>, I>>(base?: I): BuildStatusResponse {
    return BuildStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuildStatusResponse>, I>>(object: I): BuildStatusResponse {
    const message = createBaseBuildStatusResponse();
    message.statuses = Object.entries(object.statuses ?? {}).reduce<{ [key: string]: CurrentBuildStatus }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = value as CurrentBuildStatus;
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseBuildStatusResponse_StatusesEntry(): BuildStatusResponse_StatusesEntry {
  return { key: "", value: 0 };
}

export const BuildStatusResponse_StatusesEntry: MessageFns<BuildStatusResponse_StatusesEntry> = {
  encode(message: BuildStatusResponse_StatusesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int32(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuildStatusResponse_StatusesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuildStatusResponse_StatusesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuildStatusResponse_StatusesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? currentBuildStatusFromJSON(object.value) : 0,
    };
  },

  toJSON(message: BuildStatusResponse_StatusesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = currentBuildStatusToJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuildStatusResponse_StatusesEntry>, I>>(
    base?: I,
  ): BuildStatusResponse_StatusesEntry {
    return BuildStatusResponse_StatusesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuildStatusResponse_StatusesEntry>, I>>(
    object: I,
  ): BuildStatusResponse_StatusesEntry {
    const message = createBaseBuildStatusResponse_StatusesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseHashRequest(): HashRequest {
  return { definitions: [] };
}

export const HashRequest: MessageFns<HashRequest> = {
  encode(message: HashRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.definitions) {
      Definition.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HashRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHashRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.definitions.push(Definition.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HashRequest {
    return {
      definitions: globalThis.Array.isArray(object?.definitions)
        ? object.definitions.map((e: any) => Definition.fromJSON(e))
        : [],
    };
  },

  toJSON(message: HashRequest): unknown {
    const obj: any = {};
    if (message.definitions?.length) {
      obj.definitions = message.definitions.map((e) => Definition.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HashRequest>, I>>(base?: I): HashRequest {
    return HashRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HashRequest>, I>>(object: I): HashRequest {
    const message = createBaseHashRequest();
    message.definitions = object.definitions?.map((e) => Definition.fromPartial(e)) || [];
    return message;
  },
};

function createBaseHashResponse(): HashResponse {
  return { hashes: [], statuses: [] };
}

export const HashResponse: MessageFns<HashResponse> = {
  encode(message: HashResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.hashes) {
      Hash.encode(v!, writer.uint32(10).fork()).join();
    }
    writer.uint32(18).fork();
    for (const v of message.statuses) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HashResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHashResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hashes.push(Hash.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.statuses.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.statuses.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HashResponse {
    return {
      hashes: globalThis.Array.isArray(object?.hashes) ? object.hashes.map((e: any) => Hash.fromJSON(e)) : [],
      statuses: globalThis.Array.isArray(object?.statuses)
        ? object.statuses.map((e: any) => currentBuildStatusFromJSON(e))
        : [],
    };
  },

  toJSON(message: HashResponse): unknown {
    const obj: any = {};
    if (message.hashes?.length) {
      obj.hashes = message.hashes.map((e) => Hash.toJSON(e));
    }
    if (message.statuses?.length) {
      obj.statuses = message.statuses.map((e) => currentBuildStatusToJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HashResponse>, I>>(base?: I): HashResponse {
    return HashResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HashResponse>, I>>(object: I): HashResponse {
    const message = createBaseHashResponse();
    message.hashes = object.hashes?.map((e) => Hash.fromPartial(e)) || [];
    message.statuses = object.statuses?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
