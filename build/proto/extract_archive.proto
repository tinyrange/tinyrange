syntax = "proto3";

package proto;

import "source.proto";

enum ArchiveType {
  ARCHIVE_TYPE_UNSPECIFIED = 0;
  TAR = 1;
}

enum CompressionType {
  COMPRESSION_TYPE_UNSPECIFIED = 0;
  NONE = 1;
  GZIP = 2;
}

message ExtractArchiveDefinition {
  FileSource source = 1;
  ArchiveType archive_type = 2;
  CompressionType compression_type = 3;
}