`vmlinux_arm64` and `vmlinux_x86_64` are builds of the Linux kernel provided under GPL 2.0 with the syscall exception.

The kernels are built using the provided configs `linux_arm64.config` and `linux_x86_64.config`.